{
	"name": "payme-generator",
	"version": "1.0.0",
	"description": "A generator of payme.sk payment links",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "bun build index.ts --outdir=. --packages=external --target=browser --minify-syntax --sourcemap ",
		"prepublish": "npm run build"
	},
	"keywords": [
		"payme"
	],
	"author": "0CharlieCat <meow@charliecat.space>",
	"license": "ISC",
	"module": "index.js",
	"type": "module",
	"dependencies": {
		"bysquare": "^2.12.3"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}